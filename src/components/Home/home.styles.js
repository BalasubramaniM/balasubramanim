import styled from 'styled-components';

const Home = {
  Container: styled.div`
    display: flex;
    flex-direction: column;
    width: 100%;
    min-height: 100vh;
  `,
}

const Main = {
  Container: styled.main`
    flex: 1;
    `,
  Section: styled.div``,
}

export { Home, Main }
